# Note that due to VPN issues the deployment to Windows staging server 
# stage won't work if this process is not run from a self-hosted runner 
# within our VPN
name: Deploy FastAPI (No Docker)

on:
  push:
    branches:
      - main

jobs:

  selftest:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: install
        run: python -m pip install .
      - uses: pypa/gh-action-pip-audit@v1.1.0

  test-and-deploy:

    needs: selftest

    runs-on: ubuntu-latest
    
    steps:

    - uses: actions/checkout@v4

    - uses: actions/setup-python@v5
      with:
        python-version: "3.12"

    - name: Install dependencies
      run: |
        pip install -r requirements.txt
        pip install pytest

    - name: Run tests
      run: |
        pytest

    #  COMMENTED OUT UNTIL VPN ISSUE WITH STAGING IS SORTED IE GITHUB GITRUNNER CAN REACH
    #  DEPLOY TARGET
    #- name: Debug DNS
    #  run: nslookup ${{ secrets.SERVER_HOST }}    

    #- name: Deploy to Windows server
    #  uses: appleboy/ssh-action@v1
    #  with:
    #    host: ${{ secrets.SERVER_HOST }}
    #    username: ${{ secrets.SERVER_USER }}
    #    password: ${{ secrets.SERVER_SSH_KEY }}
    #    script: |
    #      powershell.exe -File C:\Users\qtnzoly\Development\fastApi\ml-resources\scripts\run_api.ps1